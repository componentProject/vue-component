import type { PageSchema } from '../types'

/**
 * 默认页面Schema
 */
export const DEFAULT_PAGE_SCHEMA: PageSchema = {
  id: 'root',
  title: '低代码页面',
  description: '使用低代码编辑器创建的页面',
  version: '1.0.0',
  components: [],
  rootId: 'root',
}
